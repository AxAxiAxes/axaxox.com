# axaxox.com

**Status:** Named concept, GitHub Pages preview live at this repo's Pages
URL; the real `axaxox.com` domain is not yet pointed at it.
**Recorded:** 2026-09-18
**Source of truth for domain inventory and product decisions:** `DOMAIN_PORTFOLIO.md` and `docs/keystone/FOUNDER_ACTION_QUEUE.md` in the AxAxiAxes/keystone-eternal-seed repository.

## Current role / status

The founder designated `axaxox.com` as the name for the "Keystone Dome /
Pomegranate / Sound" interactive concept — see
`KEYSTONE-CREATIVE-ORIGIN-000008` in
`docs/KEYSTONE_AI_CREATIVE_WORK_ORIGIN_REGISTRY.md` and
`docs/keystone/assets/interactive/README.md` in the `keystone-eternal-seed`
repository. `index.html` in this repo is that same interactive artifact
(reviewed for outbound network calls before publishing — none found),
served here via GitHub Pages.

Registrar status: still "verification required" in `DOMAIN_PORTFOLIO.md` —
publishing this preview does **not** change that. To make the real
`axaxox.com` domain show this page, the founder needs to point its DNS
(SiteGround registrar) at GitHub Pages, following the same pattern already
documented for `urartuhi.com` in `docs/memory/2026-09-17-urartuhi-gallery-site-created.md`.

## What this repo is

A GitHub namespace reservation for the domain `axaxox.com`, now also
hosting a static, client-side-only preview of the concept named after it.
No DNS, hosting-account, payment, or legal action has been taken from
repository access — this is a static GitHub Pages publish only.

## License / privacy note

This repository is public (a GitHub/GitHub Pages requirement for a free
static site), which means the page's HTML/JS/CSS is technically viewable
and copyable by any visitor's browser — this is inherent to how the open
web works and is not something any repository or hosting setting can
prevent. See `LICENSE` for the copyright/authorship terms that apply
regardless: viewing is permitted, but redistribution or derivative
republishing is not licensed.

## Before further building here

1. Confirm the domain's product purpose, owner, privacy classification, budget, and operating model beyond this preview (see the portfolio principle in `DOMAIN_PORTFOLIO.md`).
2. Get founder approval before adding any real application code, backend, or data collection.
3. Cross-link this repo from `docs/keystone/PRODUCT_BRANCH_DOMAIN_MAP.md` in `keystone-eternal-seed` if this becomes a lasting assigned role.